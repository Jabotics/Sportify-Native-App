import { StyleSheet } from "react-native";

export const errorStyles = StyleSheet.create({
  container: {
    alignItems: 'center',
    justifyContent: 'center',
    flex: 1,
    backgroundColor: 'red',
    padding: 20,
  },
})